# FUTEBOL AI — SOURCE / PROVENANCE POLICY v3.0

## Build atual

`football-dream-be-a-pro-1-221-5` é o baseline mobile da fase atual.

Existem múltiplas cópias dessa mesma build. A canonicalidade é da BUILD, não de um único path.

## Escopo local

Descoberta de fontes somente em:

- `C:\Users\dg71\Downloads`
- `C:\Users\dg71\Videos`

## Deduplicação

Comparar por SHA-256:

- APK base e split APKs;
- `libil2cpp.so`;
- `global-metadata.dat`;
- `data.unity3d` / `datapack.unity3d`;
- `controller.ctrl`;
- `spmoveconfig`;
- `spmoveactiondata`;
- `shootspeed`;
- demais arquivos de recovery relevantes.

Hashes iguais => `DUPLICATE_IDENTICAL_COPY`.
Hashes diferentes => investigar variante antes de usar.

## 92/92

O workspace histórico original 92/92 foi recuperado em Downloads, dividido em partes, com montador solto na mesma pasta. Deve ser remontado em diretório separado e revalidado antes de continuar retrabalho manual.

## 1-226-19

Versão mais recente, mas futura. Não usar na linha atual. Qualquer inspeção anterior é apenas provenance histórico. A fase oficial de comparação começa somente depois da 1-221-5 integrada/estável.
