# Current checkpoint

Updated: 2026-09-13. Project NOT COMPLETE.
Official branch: `main`.
Validated portability merge: `55e18f5c86c3b229df164b2f3661121839a805dd`.
Codex local handoff observed in the user-provided transcript: `8471a3ade0b1f5bac7b0b6c6e91a794bb5ba8fc8`; that local SHA was not published and is not GitHub authority.

Historical local implementation commits recorded by Codex: collector `5c67263fc6c25edfd0a688adf74237c720ce9662`; velocity path split `045cc8e3970583b650ead8a0dc9381c73461b3ac`. Published equivalents remain the authority.

## GitHub publication

Official remote: https://github.com/dgsiria71-jpg/UNREAL-ENGINE-FUTEBOL.git, `main`.
Reconciled remote history from `6760d3d` with local `c0d761c`; no history rewritten.
Bible text and its policies are preserved in `Docs/Bible_v3`.
Raw master LFS upload remains unperformed; code publication does not claim binary publication.

Published spmove collector content: `c1c2a452c9e1abb27c38a2e5cff9f85a2e2a06d1`.
Published checkpoint metadata: `294c322cc2953a283d9792c514cefb1924a27d0e`.
Published velocity path split content: `36365baf382afd1a3b375280cd13a5f0c0b73291`.
Fresh-checkout validation repair merged by PR #1 as `55e18f5c86c3b229df164b2f3661121839a805dd`.

## Authority

User explicitly adopted Bible v3, its Quickstart and Source Policy after full
reading. Latest direct user corrections remain highest authority; current binary,
hash and test evidence overrides stale technical claims. Windows UE5.x/C++ final,
Blueprint complementary, Blender source of truth, Neymar v1.9 PAUSED. New source
discovery only Downloads/Videos. 1-221-5 current; do not consume 1-226-19.

## Current task and discovery

Continue recovered physics from current code, not from an assumed 92/92 archive.
The six found parts were assembled and verified, but the result is Master Recovery
source consolidation and explicitly lists original 92/92 bytes as missing.
Evidence: `Recovery/Normalized/original_reassembly.json` and
`Docs/MOBILE_1_221_5_SOURCE_MAP.md`.

The native `getSpmoveIdDict` collection shape is normalized and implemented
headlessly. The open-all canonical dataset is complete at that boundary: 292
serialized configs, 290 enabled, 2 disabled, 56 child references, 68 logic
buckets, 346 entries and zero missing children. Real-player `spmoveIds`, later
eligibility/ownership, odds and RNG remain explicit unresolved inputs.

GetVHor/GetVVer are split into old/new config families and bound to 26 ARM64/layout
anchors. The next native task remains recovery of the interpolation, clamp and
arithmetic sequences in those regions, followed by final GetKickVelocity and
BALL_CONTACT velocity.

## Latest executed GREEN

### Canonical GitHub fresh checkout

GitHub Actions `Reference code validation` run `34767842579` on main merge
`55e18f5c86c3b229df164b2f3661121839a805dd` is GREEN:

- clean GitHub checkout on Ubuntu 24.04;
- CMake C++ reference/probes compile GREEN;
- CTest: 1/1 GREEN;
- Python repository suite: 63 tests, OK, 5 explicitly skipped because their full
  regeneration snapshots require preserved local source archives;
- `Tools/check_native_evidence.py`: CURRENT persisted source bindings;
- `Tools/Unreal/validate_content.py`: GREEN read-only persisted provenance checks.

The repository manifest is now UTF-8 `football.recovery.inventory.v2` and separates
committed evidence from local-regeneration artifacts. `spmove_normalized.json` and
`archive_catalog.json` are not pretended to exist in GitHub; their expected source
provenance and generators are explicit in the manifest.

### Preserved/local native evidence from Codex checkpoint

- `Tools/build_reference.cmd`: five executables compiled, C++17 /W4 /WX, 21/21 GREEN.
- `python Tools/verify_spmove_selection.py`: 4,672/4,672 native comparisons match.
- `python Tools/verify_spmove_producer.py`: preserved 2,640/2,640 native report remains source-current.
- `python Tools/analyze_spmove_inventory_collector.py`: GREEN, 23 ARM64 anchors plus IL2CPP layouts.
- `python Tools/analyze_velocity_base_regions.py`: GREEN, 26 ARM64/config-layout anchors.
- `python Tools/collect_spmove_inventory.py`: GREEN, 290 enabled configs -> 346 entries / 68 buckets.
- `python Tools/audit_feature_coverage.py`: GREEN locally, 9 systems and 5 SHA/CRC-verified source archives.
- Original v0.2 `07_TESTS`: 67/67 OK in 7.057s.
- Earlier preserved native evidence: kernels 13,685, vectors 15,956, branches 7,776.

These local-source suites are not falsely presented as rerun by GitHub Actions.

## CONFIRMED

- Source matrix: 122 critical/archive observations and 22 identical groups.
  Videos ARM64 and metadata hashes match the current recovery binary/metadata.
- Selection: highest signed child ID wins, not level/order/odds; positive father
  tie rule; noRatio low bit; missing highest config has no fallback.
- Collector: `SpmoveIDCombine` is childId +0x10/fatherId +0x14. Each enabled root
  adds `(root.id, 0)` to its logicId bucket; every child adds `(childId, root.id)`
  to the child config logicId bucket. Missing player roots are skipped.
- Runtime config dictionary excludes disabled records through enable +0x1C.
- Producer: original reset/seven flags/strict comparisons/native distance math
  match C++ with explicit external geometry/curve/membership/property outcomes.
- Stable v0.2 SHA: `7d5cabe5d974f7282ca7126d5c36c7bc71a448275cf144b73625be9fccf415ac`.
- Runtime +0x1C0 remains `vertical_accel_raw`.
- Feature audit separates recovered source, normalization, executable reference,
  Unreal integration and runtime validation.
- GitHub fresh-checkout CI is now reproducible without inventing locally generated
  recovery bytes.

## INFERRED / UNKNOWN

Managed allocation/exception details, real-player spmoveIds source/lifetime,
complete eligibility/ownership and odds/RNG remain open. The open-all artifact
uses decoded config-record order; original managed Dictionary.Values order is
not claimed. GetVHor/GetVVer old/new config-family split is confirmed, but complete
equations, GetKickVelocity and BALL_CONTACT velocity still need native-backed
integration. Original 92/92 source was not found in the reconstructed six-part
bundle; other copies remain unproven.

Unreal installation was not located by Codex: Epic launcher InstallationList was
empty and no custom registry builds were found. No UE compile/package/rendered
match or FPS result is claimed. Current C++ headless gameplay uses explicit
NewGameAuthored tuning where marked.

## Resume-source boundary

Codex was using the local ignored file `.local/il2cpp/disassembly_shoot.txt` for
the next GetVHor/GetVVer arithmetic recovery. That full listing is not committed
on GitHub. The repository contains durable traces and anchors, but not enough bytes
to honestly re-derive every native arithmetic/clamp sequence from GitHub alone.
Do not fill this gap by guessing or by treating the historical Bible as fresh
native differential evidence. When the listing or the canonical ARM64 binary is
available to the active worker, continue directly from the addresses below.

## Local game-development tooling

`game-dev` CLI 1.0.2 was installed locally by Codex from the official tagged
source. Its basic version/capabilities/doctor commands ran; doctor reported
healthy with Windows warning and without Blender/provider credentials. The
permanent local skill was recorded with content SHA-256
`e11d373f4aef10e10b13f1aa2a1d00e2a467373951ecfe839ccc6208be464409`.
The upstream full CLI suite was not GREEN on that Windows host (78 failures from
POSIX process fixtures, directory fsync and missing openssl); do not cite it as
validated for mutating asset/package workflows.

## Exact resume commands (local source workstation)

```powershell
git status --short
git rev-parse HEAD
Get-Content _CHECKPOINTS/CURRENT.md
Get-Content Recovery/Physics/EXECUTABLE_RECOVERY.md
Tools/build_reference.cmd
python Tools/verify_spmove_selection.py
python Tools/verify_spmove_producer.py
python Tools/analyze_spmove_inventory_collector.py
python Tools/collect_spmove_inventory.py
python Tools/analyze_velocity_base_regions.py
python Tools/audit_feature_coverage.py
python Tools/check_native_evidence.py
python -m unittest discover -s Tests -p "test_*.py"
python Tools/Unreal/validate_content.py
```

## Next exact step

Use `.local/il2cpp/disassembly_shoot.txt` and the existing static/native harnesses
to recover the interpolation, clamp and arithmetic sequences inside the now-split
old/new base regions of GetVHor (`0x016E6A80..0x016E84A4`) and GetVVer
(`0x016E84A4..0x016EA55C`), starting with config bindings and branch inputs before
the already validated spmove modifiers. Use `spmove_collected_open_all.json` as a
complete open-all fixture while keeping real-player inventory/eligibility explicit.
Then join final GetVHor/GetVVer, GetKickVelocity and BALL_CONTACT. Do not create
Physics v0.3 until the full gate and regression pass. Do not treat the
scaffold/headless fixture as the game.
